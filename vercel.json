{
  "buildCommand": "npx expo start --web",
  "devCommand": "npx expo start --web",
  "installCommand": "npm install",
  "outputDirectory": "dist",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}
